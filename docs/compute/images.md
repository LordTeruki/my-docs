# Available Images (Beta)

The following official images are available:

- Ubuntu (recommended)
- Debian
- Rocky Linux

These are clean images with cloud-init support for SSH key injection.

Note: Custom image upload is not available during beta.
