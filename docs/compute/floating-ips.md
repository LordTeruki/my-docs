# IPv6 Access Only (Beta)

All VMs receive a public IPv6 address automatically.

There is no public IPv4 during beta.

## What You Get

- One IPv6 address per VM
- Full inbound and outbound connectivity
- Private IPv4 internally

## Connecting

ssh user@your-ipv6-address
